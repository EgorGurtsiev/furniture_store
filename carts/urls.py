
from django.urls import path



urlpatterns = [
    # path('/', CartFromView.as_view(), name='cart'),
    # path('add/', CartFromView.as_view(), name='cart'),
    # path('remove/', CartFromView.as_view(), name='cart'),
] 